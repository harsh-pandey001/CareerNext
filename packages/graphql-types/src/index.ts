/**
 * @careernext/graphql-types
 *
 * Home for GraphQL Code Generator output. Once the API exposes its schema
 * (V1 authentication), `graphql-codegen` writes strongly-typed operations,
 * fragments, and hooks into `src/generated/` and they are re-exported here.
 *
 * This keeps the API (schema producer) and the web/mobile clients (schema
 * consumers) sharing ONE generated type surface — no duplicated GraphQL types
 * (monorepo rule: no duplicate GraphQL types).
 */

// export * from './generated/graphql';

export {};
