# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Application {
  applicationMode: ApplicationMode
  appliedAt: DateTime
  coverLetter: String
  createdAt: DateTime!
  id: ID!
  job: Job!
  notes: String
  pitchEmail: String
  status: ApplicationStatus!
  updatedAt: DateTime!
}

enum ApplicationMode {
  COMPANY_SITE
  EMAIL
  GOOGLE_FORM
  JOB_PORTAL
}

enum ApplicationStatus {
  ACCEPTED
  APPLIED
  HR_ROUND
  INTERVIEW_ROUND_1
  INTERVIEW_ROUND_2
  OA_SCHEDULED
  OFFER_RECEIVED
  REJECTED
  SAVED
}

type ApplicationStatusHistory {
  changedAt: DateTime!
  fromStatus: ApplicationStatus
  id: ID!
  toStatus: ApplicationStatus!
}

type ApplicationsAnalytics {
  funnel: [FunnelStage!]!
  interviewsByRound: [InterviewRoundBreakdown!]!
  successRates: SuccessRates!
  trend: [ApplicationsTrendPoint!]!
}

type ApplicationsTrendPoint {
  count: Int!
  period: String!
}

type AuthPayload {
  accessToken: String!
  user: User!
}

input CreateResumeDraftInput {
  content: String!
  template: String
  title: String!
}

input CustomJobInput {
  alreadyApplied: Boolean
  applicationMode: ApplicationMode
  company: String!
  contactEmail: String
  coverLetter: String
  description: String
  experienceRequired: String
  externalUrl: String
  location: String
  pitchEmail: String
  postedAt: String
  resumeVersionId: String
  skills: [String!]
  title: String!
  type: JobType
  workMode: WorkMode!
}

input DateRangeInput {
  from: DateTime
  to: DateTime
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Document {
  createdAt: DateTime!
  fileName: String!
  fileSize: Int!
  fileUrl: String!
  id: ID!
  mimeType: String!
  type: DocumentType!
}

enum DocumentType {
  CERTIFICATE
  EXPERIENCE_LETTER
  OFFER_LETTER
  RESUME
}

type Education {
  createdAt: DateTime!
  degree: String!
  endDate: DateTime
  fieldOfStudy: String
  grade: String
  id: ID!
  institution: String!
  startDate: DateTime!
}

input EducationInput {
  degree: String!
  endDate: DateTime
  fieldOfStudy: String
  grade: String
  institution: String!
  startDate: DateTime!
}

type Experience {
  company: String!
  createdAt: DateTime!
  description: String
  endDate: DateTime
  id: ID!
  isCurrent: Boolean!
  location: String
  startDate: DateTime!
  title: String!
}

input ExperienceInput {
  company: String!
  description: String
  endDate: DateTime
  isCurrent: Boolean! = false
  location: String
  startDate: DateTime!
  title: String!
}

input ForgotPasswordInput {
  email: String!
}

type FunnelStage {
  count: Int!
  status: ApplicationStatus!
}

type Interview {
  application: Application!
  createdAt: DateTime!
  id: ID!
  notes: String
  outcome: InterviewOutcome!
  round: InterviewRound!
  scheduledAt: DateTime
}

input InterviewInput {
  notes: String
  round: InterviewRound!
  scheduledAt: DateTime
}

enum InterviewOutcome {
  FAILED
  PASSED
  PENDING
}

enum InterviewRound {
  HR_ROUND
  ONLINE_ASSESSMENT
  TECHNICAL_ROUND_1
  TECHNICAL_ROUND_2
}

type InterviewRoundBreakdown {
  count: Int!
  round: InterviewRound!
}

type Job {
  applicationMode: ApplicationMode
  applicationStatus: ApplicationStatus
  company: String!
  contactEmail: String
  coverLetter: String
  createdAt: DateTime!
  description: String!
  experienceRequired: String
  externalUrl: String
  id: ID!
  location: String
  pitchEmail: String
  postedAt: DateTime
  resumeVersion: ResumeVersion
  salaryMax: Int
  salaryMin: Int
  skills: [String!]!
  title: String!
  type: JobType!
  workMode: WorkMode!
}

input JobFilterInput {
  location: String
  query: String
  type: JobType
  workMode: WorkMode
}

enum JobType {
  CONTRACT
  FULL_TIME
  INTERNSHIP
  PART_TIME
}

type Language {
  id: ID!
  name: String!
  proficiency: LanguageProficiency!
}

input LanguageInput {
  name: String!
  proficiency: LanguageProficiency!
}

enum LanguageProficiency {
  BASIC
  CONVERSATIONAL
  FLUENT
  NATIVE
}

input LoginInput {
  email: String!
  password: String!
}

type Mutation {
  addCustomJob(input: CustomJobInput!): Job!
  addEducation(input: EducationInput!): Education!
  addExperience(input: ExperienceInput!): Experience!
  addLanguage(input: LanguageInput!): Language!
  addSkill(input: SkillInput!): Skill!
  applyToJob(jobId: ID!): Job!
  createResumeDraft(input: CreateResumeDraftInput!): ResumeDraft!
  deleteDocument(documentId: ID!): Boolean!
  deleteInterview(interviewId: ID!): Boolean!
  deleteResumeDraft(id: ID!): Boolean!
  deleteResumeVersion(resumeVersionId: ID!): Boolean!
  forgotPassword(input: ForgotPasswordInput!): Boolean!
  login(input: LoginInput!): AuthPayload!
  logout: Boolean!
  markAllNotificationsRead: Boolean!
  markNotificationRead(notificationId: ID!): Notification!
  refreshToken: AuthPayload!
  register(input: RegisterInput!): AuthPayload!
  removeApplication(applicationId: ID!): Boolean!
  removeEducation(educationId: ID!): Boolean!
  removeExperience(experienceId: ID!): Boolean!
  removeLanguage(languageId: ID!): Boolean!
  removeSkill(skillId: ID!): Boolean!
  resetPassword(input: ResetPasswordInput!): Boolean!
  saveJob(jobId: ID!): Job!
  scheduleInterview(applicationId: ID!, input: InterviewInput!): Interview!
  setActiveResume(resumeVersionId: ID!): ResumeVersion!
  setInterviewOutcome(interviewId: ID!, outcome: InterviewOutcome!): Interview!
  unsaveJob(jobId: ID!): Job!
  updateApplicationStatus(applicationId: ID!, status: ApplicationStatus!): Application!
  updateCustomJob(input: CustomJobInput!, jobId: ID!): Job!
  updateEducation(educationId: ID!, input: EducationInput!): Education!
  updateExperience(experienceId: ID!, input: ExperienceInput!): Experience!
  updateInterview(input: InterviewInput!, interviewId: ID!): Interview!
  updateLanguage(input: LanguageInput!, languageId: ID!): Language!
  updateProfile(input: UpdateProfileInput!): Profile!
  updateResumeDraft(id: ID!, input: UpdateResumeDraftInput!): ResumeDraft!
  updateSkill(input: SkillInput!, skillId: ID!): Skill!
  uploadDocument(content: String!, fileName: String!, mimeType: String!, type: DocumentType!): Document!
  uploadResume(content: String!, fileName: String!, mimeType: String!): ResumeVersion!
}

type Notification {
  createdAt: DateTime!
  id: ID!
  link: String
  message: String!
  readAt: DateTime
  title: String!
  type: NotificationType!
}

enum NotificationType {
  APPLICATION_NO_RESPONSE
  APPLICATION_STATUS_CHANGED
  INTERVIEW_REMINDER
}

type PaginatedJobs {
  items: [Job!]!
  page: Int!
  pageSize: Int!
  total: Int!
  totalPages: Int!
}

input PaginationInput {
  page: Int! = 1
  pageSize: Int! = 10
}

type Profile {
  bio: String
  completionPercentage: Int!
  educations: [Education!]!
  experiences: [Experience!]!
  githubUrl: String
  headline: String
  id: ID!
  languages: [Language!]!
  linkedinUrl: String
  location: String
  portfolioUrl: String
  skills: [Skill!]!
  updatedAt: DateTime!
}

type Query {
  applicationStatusHistory(applicationId: ID!): [ApplicationStatusHistory!]!
  applicationsAnalytics(range: DateRangeInput): ApplicationsAnalytics!
  customJobDetail(jobId: ID!): Job!
  document(id: ID!): Document!

  """Liveness probe for the CareerNext API."""
  health: String!
  job(id: ID!): Job!
  jobs(filter: JobFilterInput, pagination: PaginationInput! = {page: 1, pageSize: 10}): PaginatedJobs!
  me: User!
  myApplications: [Application!]!
  myCustomJobs: [Job!]!
  myDocuments(type: DocumentType): [Document!]!
  myInterviews: [Interview!]!
  myNotifications(limit: Int! = 20): [Notification!]!
  myProfile: Profile!
  myResumeDrafts: [ResumeDraft!]!
  myResumeVersions: [ResumeVersion!]!
  resumeDraft(id: ID!): ResumeDraft!
  resumeVersion(id: ID!): ResumeVersion!
  unreadNotificationCount: Int!
  upcomingInterviews(limit: Int! = 5): [Interview!]!
}

input RegisterInput {
  email: String!
  firstName: String!
  lastName: String!
  password: String!
}

input ResetPasswordInput {
  newPassword: String!
  token: String!
}

type ResumeDraft {
  content: String!
  createdAt: DateTime!
  id: ID!
  template: String!
  title: String!
  updatedAt: DateTime!
}

type ResumeVersion {
  createdAt: DateTime!
  document: Document!
  id: ID!
  isActive: Boolean!
  version: Int!
}

type Skill {
  id: ID!
  level: SkillLevel!
  name: String!
  yearsOfExperience: Int
}

input SkillInput {
  level: SkillLevel!
  name: String!
  yearsOfExperience: Int
}

enum SkillLevel {
  ADVANCED
  BEGINNER
  EXPERT
  INTERMEDIATE
}

type SuccessRates {
  acceptanceRate: Float!
  offerRate: Float!
  totalAccepted: Int!
  totalApplications: Int!
  totalOffers: Int!
  totalRejected: Int!
}

input UpdateProfileInput {
  bio: String
  githubUrl: String
  headline: String
  linkedinUrl: String
  location: String
  portfolioUrl: String
}

input UpdateResumeDraftInput {
  content: String
  template: String
  title: String
}

type User {
  createdAt: DateTime!
  email: String!
  firstName: String!
  id: ID!
  isEmailVerified: Boolean!
  lastName: String!
  role: UserRole!
}

enum UserRole {
  ADMIN
  USER
}

enum WorkMode {
  HYBRID
  ONSITE
  REMOTE
}