/**
 * Web-only view/UI types. Domain types come from `@careernext/shared-types`
 * and generated GraphQL types from `@careernext/graphql-types` — do NOT
 * redefine those here (monorepo rule: no duplicate types).
 */
export {};
