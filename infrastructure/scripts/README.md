# infrastructure/scripts

Operational helper scripts (DB bootstrap, seed, backup, deploy helpers). Keep
them POSIX-sh compatible and idempotent.

- `dev-db.sh` — start only the local PostgreSQL container for development.
