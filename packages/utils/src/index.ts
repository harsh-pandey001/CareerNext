/**
 * @careernext/utils
 *
 * Framework-agnostic helpers shared across applications. Keep these pure and
 * side-effect free so they are safe to import in web, api, and mobile.
 *
 * RULE: Never duplicate a utility inside an app. Add it here instead
 * (monorepo rule: no duplicate utilities).
 */
export * from './date';
export * from './string';
export * from './pagination';
export * from './file';
