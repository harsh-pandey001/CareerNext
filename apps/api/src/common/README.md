# common

Shared building blocks used across feature modules: base classes, shared GraphQL
scalars, pagination args/types, constants, and reusable exceptions. Keep domain
logic thin here — cross-app helpers live in `@careernext/utils`.
