{
  "name": "@careernext/graphql-types",
  "version": "0.1.0",
  "private": true,
  "description": "GraphQL Code Generator output shared between the API schema and web/mobile clients.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "generate": "echo \"Configure graphql-codegen in V1 auth implementation\" && exit 0",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo src/generated"
  },
  "devDependencies": {
    "@careernext/typescript-config": "workspace:*",
    "typescript": "^5.6.2"
  }
}
