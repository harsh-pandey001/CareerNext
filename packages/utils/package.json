{
  "name": "@careernext/utils",
  "version": "0.1.0",
  "private": true,
  "description": "Framework-agnostic shared utilities for the CareerNext monorepo.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "echo \"No tests yet\" && exit 0",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@careernext/shared-types": "workspace:*"
  },
  "devDependencies": {
    "@careernext/eslint-config": "workspace:*",
    "@careernext/typescript-config": "workspace:*",
    "typescript": "^5.6.2"
  }
}
