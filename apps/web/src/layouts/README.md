# layouts

Structural page shells shared across routes:

- `AuthLayout` — centered card shell for login/register/forgot-password.
- `DashboardLayout` — sidebar + topbar shell for authenticated app pages.

Layouts compose providers/navigation only; they hold no feature logic.
